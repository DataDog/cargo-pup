
///
/// This function is too long
///
pub fn free_function_too_long() -> i32 {
    // A function body that should be
    // less than 5 lines long but isn't.
    let a = 1+1;
    let b = 1+1;
    let c = 1+1;
    a + b + c
}


struct MyStruct{}

///
/// This function is too long
///
impl MyStruct{
    fn impl_function_too_long() -> i32 {
    // A function body that should be
    // less than 5 lines long but isn't.
    let a = 1+1;
    let b = 1+1;
    let c = 1+1;
    a + b + c
    }
}