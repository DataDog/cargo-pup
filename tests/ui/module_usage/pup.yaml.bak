# Configuration for AllowOnly rule type in module_usage lint 
allow_only_rule:
  type: module_usage
  modules:
    - "^test::test_allow_only$"
  rules:
    - type: AllowOnly
      allowed_modules:
        - "std::fmt"
        - "std::io"
      severity: Error