# Empty module enforcer for the entire crate
#
# This rule ensures that mod.rs files are kept empty, serving only as module 
# organization files, and all actual implementations are in separate files.
#
# Crate: {0}
#
# Parameters:
#   modules: list of regex patterns for modules to check
#   severity: Error or Warn
#
type: empty_mod
modules:
  - "^{1}"  # Apply to entire crate
severity: Warn