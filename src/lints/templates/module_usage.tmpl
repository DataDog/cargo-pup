# Module usage restrictions
#
# This rule allows you to control how modules are imported and used.
# You can restrict wildcard imports, deny specific modules, or only
# allow certain modules to be imported.
#
# Crate: {0}
#
# Parameters:
#   modules: list of regex patterns for modules to apply restrictions to
#   rules: list of rule configurations (see examples below)
#
type: module_usage
modules:
  - "^{1}"  # Apply to entire crate
rules:
  - type: DenyWildcard
    severity: Warn

# Example of other configurations:
#
# deny_specific_modules:
#   type: module_usage
#   modules:
#     - "^{1}::core$"  # Apply to core module
#   rules:
#     - type: Deny
#       denied_modules:
#         - "^std::collections"
#         - "^tokio::"
#       severity: Error
